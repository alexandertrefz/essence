// NOTE: The `Optional` covering Namespace is implemented entirely in Essence —
// its one Method, `otherwise`, lives in `src/stdlib/Optional.es`. This module
// stays because the Rewriter imports one per builtin Namespace by name; it just
// has nothing native left to export.
export {}

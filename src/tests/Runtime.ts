/// <reference path="../../typings/index.d.ts" />

import { } from '../Interfaces'

describe('Runtime', () => {
	let assert = require('assert')

	describe('something', () => {
		assert.ok(true)
	})
})

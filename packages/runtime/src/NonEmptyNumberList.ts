// NOTE: The runtime module of the `NonEmptyNumberList` Namespace — the
// aggregates a proven mixed List of Numbers answers bare. Every Method of it is
// written in Essence (`packages/standard-library/sources/NumberList.es`),
// delegating to the `Number` static's own proven entry, so there is nothing
// native here. The module stays because the Rewriter imports one per builtin
// Namespace by name, exactly as `NestedOptional.ts` does.
export {}

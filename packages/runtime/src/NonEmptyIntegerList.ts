// NOTE: The runtime module of the `NonEmptyIntegerList` Namespace — the
// aggregates a proven List of Integers answers bare. Every Method of it is
// written in Essence (`packages/standard-library/sources/NumberList.es`),
// collapsing a `Number` static's Optional against an item the receiver
// certainly holds, so there is nothing native here. The module stays because
// the Rewriter imports one per builtin Namespace by name, exactly as
// `NestedOptional.ts` does.
export {}

// NOTE: The runtime module of the `NonEmptyKeyedNumberList` Namespace — the
// keyed mean a proven List answers bare
// (`packages/standard-library/sources/NumberList.es`). Its one Method is
// written in Essence, mapping the key over the receiver and averaging what it
// reads, so there is nothing native here. The module stays because the
// Rewriter imports one per builtin Namespace by name, exactly as
// `KeyedNumberList.ts` does.
export {}

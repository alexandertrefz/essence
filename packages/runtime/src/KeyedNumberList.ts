// NOTE: The runtime module of the `KeyedNumberList` Namespace — the numeric
// aggregates any List answers through a key
// (`packages/standard-library/sources/NumberList.es`). Every Method of it is
// written in Essence, delegating to a `Number` static, so there is nothing
// native here. The module stays because the Rewriter imports one per builtin
// Namespace by name, exactly as `IntegerList.ts` does.
export {}

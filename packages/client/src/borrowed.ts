// NOTE: The names a generated declaration file BORROWS from this package — every
// `import type { … } from "@essence-lang/client"` a `.d.es.ts` writes names one
// of these, and nothing else. Gathered in one module so that a test can point a
// consumer straight at it instead of at the package's whole root, and so that
// the list of what a generated file may reach for is a file rather than a
// search.
export type { Input } from "./input"
export { EssenceRational } from "./rational"
